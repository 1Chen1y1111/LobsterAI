# coworkRunner.ts 方法与声明作用说明

- 文件：[src/main/libs/coworkRunner.ts](src/main/libs/coworkRunner.ts)
- 顶层声明数量：70
- 类数量：1
- 类成员总数：115

## 顶层声明

- [variable SANDBOX_ALLOWED_ENV_KEYS](src/main/libs/coworkRunner.ts#L33)：用于沙箱模式路径、环境或执行行为控制。
- [variable SANDBOX_SKILLS_MOUNT_TAG](src/main/libs/coworkRunner.ts#L50)：用于沙箱模式路径、环境或执行行为控制。
- [variable SANDBOX_SKILLS_GUEST_PATH](src/main/libs/coworkRunner.ts#L54)：用于沙箱模式路径、环境或执行行为控制。
- [variable SANDBOX_SKILLS_GUEST_PATH_WINDOWS](src/main/libs/coworkRunner.ts#L56)：用于沙箱模式路径、环境或执行行为控制。
- [variable SANDBOX_WORKSPACE_GUEST_ROOT](src/main/libs/coworkRunner.ts#L58)：用于沙箱模式路径、环境或执行行为控制。
- [variable SANDBOX_WORKSPACE_LEGACY_ROOT](src/main/libs/coworkRunner.ts#L60)：用于沙箱模式路径、环境或执行行为控制。
- [variable ATTACHMENT_LINE_RE](src/main/libs/coworkRunner.ts#L62)：用于文本规则匹配与输入识别。
- [variable INFERRED_FILE_REFERENCE_RE](src/main/libs/coworkRunner.ts#L64)：用于文本规则匹配与输入识别。
- [variable SANDBOX_ATTACHMENT_DIR](src/main/libs/coworkRunner.ts#L66)：用于沙箱模式路径、环境或执行行为控制。
- [variable LEGACY_SKILLS_ROOT_HINTS](src/main/libs/coworkRunner.ts#L68)：用于技能路径解析、挂载与调用链适配。
- [variable INFERRED_FILE_SEARCH_IGNORE](src/main/libs/coworkRunner.ts#L70)：常量/配置声明，作为运行策略与默认参数来源。
- [variable SANDBOX_HISTORY_MAX_MESSAGES](src/main/libs/coworkRunner.ts#L72)：用于阈值/限流控制，防止性能或内存压力。
- [variable SANDBOX_HISTORY_MAX_TOTAL_CHARS](src/main/libs/coworkRunner.ts#L74)：用于阈值/限流控制，防止性能或内存压力。
- [variable SANDBOX_HISTORY_MAX_MESSAGE_CHARS](src/main/libs/coworkRunner.ts#L76)：用于阈值/限流控制，防止性能或内存压力。
- [variable LOCAL_HISTORY_MAX_MESSAGES](src/main/libs/coworkRunner.ts#L78)：用于阈值/限流控制，防止性能或内存压力。
- [variable LOCAL_HISTORY_MAX_TOTAL_CHARS](src/main/libs/coworkRunner.ts#L80)：用于阈值/限流控制，防止性能或内存压力。
- [variable LOCAL_HISTORY_MAX_MESSAGE_CHARS](src/main/libs/coworkRunner.ts#L82)：用于阈值/限流控制，防止性能或内存压力。
- [variable STREAM_UPDATE_THROTTLE_MS](src/main/libs/coworkRunner.ts#L84)：用于超时控制，避免流程无限等待。
- [variable STREAMING_TEXT_MAX_CHARS](src/main/libs/coworkRunner.ts#L86)：用于阈值/限流控制，防止性能或内存压力。
- [variable STREAMING_THINKING_MAX_CHARS](src/main/libs/coworkRunner.ts#L88)：用于阈值/限流控制，防止性能或内存压力。
- [variable TOOL_RESULT_MAX_CHARS](src/main/libs/coworkRunner.ts#L90)：用于阈值/限流控制，防止性能或内存压力。
- [variable FINAL_RESULT_MAX_CHARS](src/main/libs/coworkRunner.ts#L92)：用于阈值/限流控制，防止性能或内存压力。
- [variable STDERR_TAIL_MAX_CHARS](src/main/libs/coworkRunner.ts#L94)：用于阈值/限流控制，防止性能或内存压力。
- [variable SDK_STARTUP_TIMEOUT_MS](src/main/libs/coworkRunner.ts#L96)：用于超时控制，避免流程无限等待。
- [variable SDK_STARTUP_TIMEOUT_WITH_USER_MCP_MS](src/main/libs/coworkRunner.ts#L98)：用于超时控制，避免流程无限等待。
- [variable STDERR_FATAL_PATTERNS](src/main/libs/coworkRunner.ts#L100)：用于文本规则匹配与输入识别。
- [variable CONTENT_TRUNCATED_HINT](src/main/libs/coworkRunner.ts#L117)：常量/配置声明，作为运行策略与默认参数来源。
- [variable TOOL_INPUT_PREVIEW_MAX_CHARS](src/main/libs/coworkRunner.ts#L119)：用于阈值/限流控制，防止性能或内存压力。
- [variable TOOL_INPUT_PREVIEW_MAX_DEPTH](src/main/libs/coworkRunner.ts#L121)：用于阈值/限流控制，防止性能或内存压力。
- [variable TOOL_INPUT_PREVIEW_MAX_KEYS](src/main/libs/coworkRunner.ts#L123)：用于阈值/限流控制，防止性能或内存压力。
- [variable TOOL_INPUT_PREVIEW_MAX_ITEMS](src/main/libs/coworkRunner.ts#L125)：用于阈值/限流控制，防止性能或内存压力。
- [variable SKILLS_MARKER](src/main/libs/coworkRunner.ts#L127)：用于技能路径解析、挂载与调用链适配。
- [variable TASK_WORKSPACE_CONTAINER_DIR](src/main/libs/coworkRunner.ts#L129)：常量/配置声明，作为运行策略与默认参数来源。
- [variable PERMISSION_RESPONSE_TIMEOUT_MS](src/main/libs/coworkRunner.ts#L131)：用于超时控制，避免流程无限等待。
- [variable DELETE_TOOL_NAMES](src/main/libs/coworkRunner.ts#L133)：常量/配置声明，作为运行策略与默认参数来源。
- [variable SAFETY_APPROVAL_ALLOW_OPTION](src/main/libs/coworkRunner.ts#L135)：常量/配置声明，作为运行策略与默认参数来源。
- [variable SAFETY_APPROVAL_DENY_OPTION](src/main/libs/coworkRunner.ts#L137)：常量/配置声明，作为运行策略与默认参数来源。
- [variable DELETE_COMMAND_RE](src/main/libs/coworkRunner.ts#L139)：用于文本规则匹配与输入识别。
- [variable FIND_DELETE_COMMAND_RE](src/main/libs/coworkRunner.ts#L141)：用于文本规则匹配与输入识别。
- [variable GIT_CLEAN_COMMAND_RE](src/main/libs/coworkRunner.ts#L143)：用于文本规则匹配与输入识别。
- [variable PYTHON_BASH_COMMAND_RE](src/main/libs/coworkRunner.ts#L145)：用于文本规则匹配与输入识别。
- [variable PYTHON_PIP_BASH_COMMAND_RE](src/main/libs/coworkRunner.ts#L147)：用于文本规则匹配与输入识别。
- [variable MEMORY_REQUEST_TAIL_SPLIT_RE](src/main/libs/coworkRunner.ts#L149)：用于文本规则匹配与输入识别。
- [variable MEMORY_PROCEDURAL_TEXT_RE](src/main/libs/coworkRunner.ts#L152)：用于文本规则匹配与输入识别。
- [variable MEMORY_ASSISTANT_STYLE_TEXT_RE](src/main/libs/coworkRunner.ts#L155)：用于文本规则匹配与输入识别。
- [variable WINDOWS_HIDE_INIT_SCRIPT_NAME](src/main/libs/coworkRunner.ts#L157)：常量/配置声明，作为运行策略与默认参数来源。
- [variable WINDOWS_HIDE_INIT_SCRIPT_CONTENT](src/main/libs/coworkRunner.ts#L159)：常量/配置声明，作为运行策略与默认参数来源。
- [function ensureWindowsChildProcessHideInitScript](src/main/libs/coworkRunner.ts#L223)：确保前置条件成立，不满足时自动补齐。
- [function prependNodeRequireArg](src/main/libs/coworkRunner.ts#L252)：辅助函数，封装可复用的独立逻辑。
- [function escapeRegExp](src/main/libs/coworkRunner.ts#L262)：用于文本规则匹配与输入识别。
- [function findSkillsMarkerIndex](src/main/libs/coworkRunner.ts#L267)：用于技能路径解析、挂载与调用链适配。
- [function isPathWithin](src/main/libs/coworkRunner.ts#L272)：布尔判定函数，用于分支控制。
- [function resolveSkillPathFromRoots](src/main/libs/coworkRunner.ts#L284)：用于技能路径解析、挂载与调用链适配。
- [function detectBinaryMagic](src/main/libs/coworkRunner.ts#L333)：检测当前输入或文件类型并给出判断。
- [function summarizeRuntimeBinary](src/main/libs/coworkRunner.ts#L352)：提炼关键信息用于日志和故障诊断。
- [function persistSandboxSpawnDiagnostics](src/main/libs/coworkRunner.ts#L372)：用于沙箱模式路径、环境或执行行为控制。
- [function formatSandboxSpawnError](src/main/libs/coworkRunner.ts#L386)：用于沙箱模式路径、环境或执行行为控制。
- [function summarizeEndpointForLog](src/main/libs/coworkRunner.ts#L420)：提炼关键信息用于日志和故障诊断。
- [function extractHostFromUrl](src/main/libs/coworkRunner.ts#L441)：从原始文本或对象中提取关键字段。
- [function mergeNoProxyList](src/main/libs/coworkRunner.ts#L454)：合并多路输入并执行去重或优先级策略。
- [interface CoworkRunnerEvents](src/main/libs/coworkRunner.ts#L485)：定义结构契约，约束对象字段与类型。
- [interface PermissionRequest](src/main/libs/coworkRunner.ts#L499)：定义结构契约，约束对象字段与类型。
- [interface ActiveSession](src/main/libs/coworkRunner.ts#L509)：定义结构契约，约束对象字段与类型。
- [interface PendingPermission](src/main/libs/coworkRunner.ts#L585)：定义结构契约，约束对象字段与类型。
- [interface SandboxPendingPermission](src/main/libs/coworkRunner.ts#L593)：定义结构契约，约束对象字段与类型。
- [interface QueuedTurnMemoryUpdate](src/main/libs/coworkRunner.ts#L601)：定义结构契约，约束对象字段与类型。
- [type AttachmentEntry](src/main/libs/coworkRunner.ts#L625)：定义类型别名，复用复杂类型组合。
- [type SandboxSkillRewriteOptions](src/main/libs/coworkRunner.ts#L635)：定义类型别名，复用复杂类型组合。
- [type SandboxSkillEntry](src/main/libs/coworkRunner.ts#L645)：定义类型别名，复用复杂类型组合。
- [type SandboxSkillRootMount](src/main/libs/coworkRunner.ts#L657)：定义类型别名，复用复杂类型组合。

## 类 CoworkRunner

- [class CoworkRunner](src/main/libs/coworkRunner.ts#L667)：承载会话运行主流程与工具编排逻辑。

### 成员

- [property store](src/main/libs/coworkRunner.ts#L669)：类级状态字段，用于跨方法共享运行上下文。
- [property activeSessions](src/main/libs/coworkRunner.ts#L671)：类级状态字段，用于跨方法共享运行上下文。
- [property pendingPermissions](src/main/libs/coworkRunner.ts#L673)：类级状态字段，用于跨方法共享运行上下文。
- [property sandboxPermissions](src/main/libs/coworkRunner.ts#L675)：类级状态字段，用于跨方法共享运行上下文。
- [property stoppedSessions](src/main/libs/coworkRunner.ts#L677)：类级状态字段，用于跨方法共享运行上下文。
- [property turnMemoryQueue](src/main/libs/coworkRunner.ts#L679)：类级状态字段，用于跨方法共享运行上下文。
- [property turnMemoryQueueKeys](src/main/libs/coworkRunner.ts#L681)：类级状态字段，用于跨方法共享运行上下文。
- [property lastTurnMemoryKeyBySession](src/main/libs/coworkRunner.ts#L683)：类级状态字段，用于跨方法共享运行上下文。
- [property drainingTurnMemoryQueue](src/main/libs/coworkRunner.ts#L685)：类级状态字段，用于跨方法共享运行上下文。
- [property mcpServerProvider](src/main/libs/coworkRunner.ts#L687)：类级状态字段，用于跨方法共享运行上下文。
- [constructor constructor](src/main/libs/coworkRunner.ts#L705)：初始化实例依赖与默认运行状态。
- [method setMcpServerProvider](src/main/libs/coworkRunner.ts#L711)：类方法，参与会话生命周期与工具执行流程。
- [method isSessionStopRequested](src/main/libs/coworkRunner.ts#L733)：布尔判定函数，用于分支控制。
- [method applyTurnMemoryUpdatesForSession](src/main/libs/coworkRunner.ts#L738)：用于记忆提取、判定、更新或回写流程。
- [method getSandboxUnavailableFallbackNotice](src/main/libs/coworkRunner.ts#L787)：用于沙箱模式路径、环境或执行行为控制。
- [method drainTurnMemoryQueue](src/main/libs/coworkRunner.ts#L795)：用于记忆提取、判定、更新或回写流程。
- [method escapeXml](src/main/libs/coworkRunner.ts#L843)：清洗/转义数据，降低注入和解析风险。
- [method buildUserMemoriesXml](src/main/libs/coworkRunner.ts#L848)：构建目标数据结构或请求参数。
- [method formatChatSearchOutput](src/main/libs/coworkRunner.ts#L888)：格式化输出内容，供日志/UI/提示词消费。
- [method formatMemoryUserEditsResult](src/main/libs/coworkRunner.ts#L922)：用于记忆提取、判定、更新或回写流程。
- [method sanitizeMemoryToolText](src/main/libs/coworkRunner.ts#L953)：用于记忆提取、判定、更新或回写流程。
- [method validateMemoryToolText](src/main/libs/coworkRunner.ts#L967)：用于记忆提取、判定、更新或回写流程。
- [method runConversationSearchTool](src/main/libs/coworkRunner.ts#L991)：类方法，参与会话生命周期与工具执行流程。
- [method runRecentChatsTool](src/main/libs/coworkRunner.ts#L1009)：类方法，参与会话生命周期与工具执行流程。
- [method runMemoryUserEditsTool](src/main/libs/coworkRunner.ts#L1027)：用于记忆提取、判定、更新或回写流程。
- [method isDirectory](src/main/libs/coworkRunner.ts#L1210)：布尔判定函数，用于分支控制。
- [method extractHostSkillRootsFromPrompt](src/main/libs/coworkRunner.ts#L1219)：用于技能路径解析、挂载与调用链适配。
- [method collectHostSkillsRoots](src/main/libs/coworkRunner.ts#L1258)：用于技能路径解析、挂载与调用链适配。
- [method collectSandboxSkillEntries](src/main/libs/coworkRunner.ts#L1292)：用于沙箱模式路径、环境或执行行为控制。
- [method resolveSandboxSkillsConfig](src/main/libs/coworkRunner.ts#L1367)：用于沙箱模式路径、环境或执行行为控制。
- [method buildSandboxEnv](src/main/libs/coworkRunner.ts#L1486)：用于沙箱模式路径、环境或执行行为控制。
- [method parseAttachmentEntries](src/main/libs/coworkRunner.ts#L1564)：类方法，参与会话生命周期与工具执行流程。
- [method resolveAttachmentPath](src/main/libs/coworkRunner.ts#L1585)：解析输入并归一化为可执行结果。
- [method toWorkspaceRelativePromptPath](src/main/libs/coworkRunner.ts#L1595)：类方法，参与会话生命周期与工具执行流程。
- [method stageExternalAttachment](src/main/libs/coworkRunner.ts#L1607)：类方法，参与会话生命周期与工具执行流程。
- [method pushStagedAttachmentsToSandbox](src/main/libs/coworkRunner.ts#L1650)：用于沙箱模式路径、环境或执行行为控制。
- [method preparePromptForSandbox](src/main/libs/coworkRunner.ts#L1707)：用于沙箱模式路径、环境或执行行为控制。
- [method findWorkspaceFileByName](src/main/libs/coworkRunner.ts#L1760)：类方法，参与会话生命周期与工具执行流程。
- [method resolveInferredFilePath](src/main/libs/coworkRunner.ts#L1803)：解析输入并归一化为可执行结果。
- [method inferReferencedWorkspaceFiles](src/main/libs/coworkRunner.ts#L1824)：类方法，参与会话生命周期与工具执行流程。
- [method augmentPromptWithReferencedWorkspaceFiles](src/main/libs/coworkRunner.ts#L1865)：类方法，参与会话生命周期与工具执行流程。
- [method truncateSandboxHistoryContent](src/main/libs/coworkRunner.ts#L1893)：用于沙箱模式路径、环境或执行行为控制。
- [method truncateLargeContent](src/main/libs/coworkRunner.ts#L1906)：类方法，参与会话生命周期与工具执行流程。
- [method sanitizeToolPayload](src/main/libs/coworkRunner.ts#L1914)：清洗/转义数据，降低注入和解析风险。
- [method appendStreamingDelta](src/main/libs/coworkRunner.ts#L1989)：用于流式输出的拼接、节流或增量更新。
- [method shouldEmitStreamingUpdate](src/main/libs/coworkRunner.ts#L2022)：用于流式输出的拼接、节流或增量更新。
- [method formatSandboxHistoryMessage](src/main/libs/coworkRunner.ts#L2039)：用于沙箱模式路径、环境或执行行为控制。
- [method buildHistoryBlocks](src/main/libs/coworkRunner.ts#L2056)：构建目标数据结构或请求参数。
- [method buildSandboxHistoryBlocks](src/main/libs/coworkRunner.ts#L2115)：用于沙箱模式路径、环境或执行行为控制。
- [method injectSandboxHistoryPrompt](src/main/libs/coworkRunner.ts#L2124)：用于沙箱模式路径、环境或执行行为控制。
- [method injectLocalHistoryPrompt](src/main/libs/coworkRunner.ts#L2154)：类方法，参与会话生命周期与工具执行流程。
- [method rewriteSkillPathsForSandbox](src/main/libs/coworkRunner.ts#L2185)：用于沙箱模式路径、环境或执行行为控制。
- [method rewriteSkillLocationForSandbox](src/main/libs/coworkRunner.ts#L2226)：用于沙箱模式路径、环境或执行行为控制。
- [method rewriteSkillReferencesForSandbox](src/main/libs/coworkRunner.ts#L2288)：用于沙箱模式路径、环境或执行行为控制。
- [method buildSandboxSkillRootMappings](src/main/libs/coworkRunner.ts#L2355)：用于沙箱模式路径、环境或执行行为控制。
- [method mapHostSkillPathToSandboxPath](src/main/libs/coworkRunner.ts#L2410)：用于沙箱模式路径、环境或执行行为控制。
- [method normalizeWorkspaceRoot](src/main/libs/coworkRunner.ts#L2440)：类方法，参与会话生命周期与工具执行流程。
- [method inferWorkspaceRootFromSessionCwd](src/main/libs/coworkRunner.ts#L2453)：类方法，参与会话生命周期与工具执行流程。
- [method resolveHostWorkspaceFallback](src/main/libs/coworkRunner.ts#L2467)：解析输入并归一化为可执行结果。
- [method mapSandboxGuestCwdToHost](src/main/libs/coworkRunner.ts#L2485)：用于沙箱模式路径、环境或执行行为控制。
- [method resolveSessionCwdForExecution](src/main/libs/coworkRunner.ts#L2531)：解析输入并归一化为可执行结果。
- [method formatLocalDateTime](src/main/libs/coworkRunner.ts#L2566)：格式化输出内容，供日志/UI/提示词消费。
- [method formatLocalIsoWithoutTimezone](src/main/libs/coworkRunner.ts#L2573)：格式化输出内容，供日志/UI/提示词消费。
- [method formatUtcOffset](src/main/libs/coworkRunner.ts#L2580)：格式化输出内容，供日志/UI/提示词消费。
- [method buildLocalTimeContextPrompt](src/main/libs/coworkRunner.ts#L2595)：构建目标数据结构或请求参数。
- [method buildWindowsEncodingPrompt](src/main/libs/coworkRunner.ts#L2620)：构建目标数据结构或请求参数。
- [method buildWindowsBundledRuntimePrompt](src/main/libs/coworkRunner.ts#L2635)：构建目标数据结构或请求参数。
- [method buildWorkspaceSafetyPrompt](src/main/libs/coworkRunner.ts#L2653)：构建目标数据结构或请求参数。
- [method composeEffectiveSystemPrompt](src/main/libs/coworkRunner.ts#L2683)：类方法，参与会话生命周期与工具执行流程。
- [method buildPromptPrefix](src/main/libs/coworkRunner.ts#L2723)：构建目标数据结构或请求参数。
- [method extractToolCommand](src/main/libs/coworkRunner.ts#L2732)：从原始文本或对象中提取关键字段。
- [method isDeleteOperation](src/main/libs/coworkRunner.ts#L2739)：布尔判定函数，用于分支控制。
- [method truncateCommandPreview](src/main/libs/coworkRunner.ts#L2759)：类方法，参与会话生命周期与工具执行流程。
- [method buildSafetyQuestionInput](src/main/libs/coworkRunner.ts#L2767)：构建目标数据结构或请求参数。
- [method isSafetyApproval](src/main/libs/coworkRunner.ts#L2798)：布尔判定函数，用于分支控制。
- [method requestSafetyApproval](src/main/libs/coworkRunner.ts#L2829)：类方法，参与会话生命周期与工具执行流程。
- [method enforceToolSafetyPolicy](src/main/libs/coworkRunner.ts#L2856)：类方法，参与会话生命周期与工具执行流程。
- [method isPythonRelatedBashCommand](src/main/libs/coworkRunner.ts#L2881)：布尔判定函数，用于分支控制。
- [method isPythonPipBashCommand](src/main/libs/coworkRunner.ts#L2889)：布尔判定函数，用于分支控制。
- [method ensureWindowsPythonRuntimeForCommand](src/main/libs/coworkRunner.ts#L2897)：确保前置条件成立，不满足时自动补齐。
- [method startSession](src/main/libs/coworkRunner.ts#L2936)：类方法，参与会话生命周期与工具执行流程。
- [method continueSession](src/main/libs/coworkRunner.ts#L3059)：类方法，参与会话生命周期与工具执行流程。
- [method stopSession](src/main/libs/coworkRunner.ts#L3167)：类方法，参与会话生命周期与工具执行流程。
- [method respondToPermission](src/main/libs/coworkRunner.ts#L3197)：用于权限请求、审批或拒绝处理。
- [method handleHostToolExecution](src/main/libs/coworkRunner.ts#L3234)：类方法，参与会话生命周期与工具执行流程。
- [method writeSandboxHostToolResponse](src/main/libs/coworkRunner.ts#L3314)：用于沙箱模式路径、环境或执行行为控制。
- [method writeSandboxPermissionResponse](src/main/libs/coworkRunner.ts#L3337)：用于沙箱模式路径、环境或执行行为控制。
- [method runClaudeCodeLocal](src/main/libs/coworkRunner.ts#L3360)：类方法，参与会话生命周期与工具执行流程。
- [method runClaudeCode](src/main/libs/coworkRunner.ts#L4290)：类方法，参与会话生命周期与工具执行流程。
- [method runClaudeCodeInSandbox](src/main/libs/coworkRunner.ts#L4439)：用于沙箱模式路径、环境或执行行为控制。
- [method continueSandboxTurn](src/main/libs/coworkRunner.ts#L5133)：用于沙箱模式路径、环境或执行行为控制。
- [method resolveAutoRoutingForSandbox](src/main/libs/coworkRunner.ts#L5400)：用于沙箱模式路径、环境或执行行为控制。
- [method enforceSandboxWorkspacePrompt](src/main/libs/coworkRunner.ts#L5524)：用于沙箱模式路径、环境或执行行为控制。
- [method resolveAssistantEventError](src/main/libs/coworkRunner.ts#L5546)：解析输入并归一化为可执行结果。
- [method normalizeSdkError](src/main/libs/coworkRunner.ts#L5572)：类方法，参与会话生命周期与工具执行流程。
- [method handleClaudeEvent](src/main/libs/coworkRunner.ts#L5588)：类方法，参与会话生命周期与工具执行流程。
- [method handleStreamEvent](src/main/libs/coworkRunner.ts#L5917)：用于流式输出的拼接、节流或增量更新。
- [method finalizeStreamingContent](src/main/libs/coworkRunner.ts#L6140)：用于流式输出的拼接、节流或增量更新。
- [method waitForPermissionResponse](src/main/libs/coworkRunner.ts#L6174)：用于权限请求、审批或拒绝处理。
- [method clearPendingPermissions](src/main/libs/coworkRunner.ts#L6217)：用于权限请求、审批或拒绝处理。
- [method clearSandboxPermissions](src/main/libs/coworkRunner.ts#L6227)：用于沙箱模式路径、环境或执行行为控制。
- [method waitForVmReady](src/main/libs/coworkRunner.ts#L6236)：类方法，参与会话生命周期与工具执行流程。
- [method readSandboxStream](src/main/libs/coworkRunner.ts#L6411)：用于沙箱模式路径、环境或执行行为控制。
- [method addSystemMessage](src/main/libs/coworkRunner.ts#L6474)：类方法，参与会话生命周期与工具执行流程。
- [method findAttachmentsOutsideCwd](src/main/libs/coworkRunner.ts#L6491)：类方法，参与会话生命周期与工具执行流程。
- [method getMessageById](src/main/libs/coworkRunner.ts#L6515)：类方法，参与会话生命周期与工具执行流程。
- [method updateMessageMerged](src/main/libs/coworkRunner.ts#L6522)：类方法，参与会话生命周期与工具执行流程。
- [method persistFinalResult](src/main/libs/coworkRunner.ts#L6543)：类方法，参与会话生命周期与工具执行流程。
- [method extractText](src/main/libs/coworkRunner.ts#L6628)：从原始文本或对象中提取关键字段。
- [method formatToolResultContent](src/main/libs/coworkRunner.ts#L6664)：格式化输出内容，供日志/UI/提示词消费。
- [method handleError](src/main/libs/coworkRunner.ts#L6680)：类方法，参与会话生命周期与工具执行流程。
- [method isSessionActive](src/main/libs/coworkRunner.ts#L6697)：布尔判定函数，用于分支控制。
- [method getSessionConfirmationMode](src/main/libs/coworkRunner.ts#L6702)：类方法，参与会话生命周期与工具执行流程。
- [method getActiveSessionIds](src/main/libs/coworkRunner.ts#L6707)：类方法，参与会话生命周期与工具执行流程。
- [method stopAllSessions](src/main/libs/coworkRunner.ts#L6712)：类方法，参与会话生命周期与工具执行流程。